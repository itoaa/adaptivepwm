# Host CLI tools (optional)

Hjälpverktyg mot enhetens UART-CLI. **Firmware-sanningen** är kommandona i `App/Src/cli_commands.c` (`help` på serien).

Bygg/kör efter behov; vissa filer kan vara ofullständiga (placeholders). Prioritet är firmware + HIL, inte denna mapp.
